---
layout: default
title: Archives for %str_month%, %year%
---
%h1==Archives for #{to_month(page.month)}, #{page.year}
- page.archive_posts.reverse.each do |post|
  .post
    .title
      %h1
        %a{:href=>"#{post.url}", :title=>"#{post.title}"}=post.title
      .info
        %p==Posted on: #{post.date.strftime('%b %d %Y')}
        %ul.post_tags
          %li.first Tagged as:
          - post.categories.each do |tag|
            %li
              %a{:href=>"/category/#{category_slug(tag)}/index.html", :title=>"#{category_capitalize(tag)}"}=category_capitalize(tag)
    .body= post.content
