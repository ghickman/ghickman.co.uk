---
layout: default
post_page: true
---
.post
    .title
        %h1#post_title== #{page.title}
        .info
            %p= #{Posted on:} page.date.strftime('%b %d %Y')
            %ul.post_tags
                %li.first
                
                - for tag in page.categories
                %li= #{tag}

    content